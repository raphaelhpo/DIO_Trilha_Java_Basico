package Set;
import java.util.HashSet;
import java.util.Set;

public class ConteudoSET {
        Set<String> set = new HashSet<>();
        /*SET não pode ter elementos dupicados, diferente dos list;

        Existem três tipos diferentes de SET, são eles:
        HashSet: Ao adicionar um elemento, ele é inserido em ordem aleatória.
        LinkedHashSet: Consegue salvar os elementos em ordem de sessão, parecido com o linked list.
        TreeSet: Consegue indicar a forma como fica ordenado.
        
        Set não libera acesso aleatório a um elemento na coleção;

        Para recorrer os elementos em um set, basta utilizar ou um Iterator ou um foreach;
        
        */


}
