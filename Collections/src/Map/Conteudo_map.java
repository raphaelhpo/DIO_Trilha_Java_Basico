package Map;

public class Conteudo_map {
    public static void main(String[] args) {
        /*
         *Map é utilizado para fazer istas de dados (chave, valor);
         *A chave do map não pode ser duplicada;
         *Existem três implementações para MAP: hashmap, treeMap, linkedHashMap;
         *As operações básicas do Map são:
         *  put (inserir ou atualizar),
         *  get (obter),
         *  containsKey (verificar se contém uma chave),
         *  containsValue (verificar se contém um valor),
         *  size (obter o tamanho) e
         *  isEmpty (verificar se está vazio).
         */
    }
}
